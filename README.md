# A simple sveltekit chatbot app to leverage the power of LLMs af Scale (Claude @ https://www.anthropic.com/)

api route in backend is hidden to avoid abuse, actual server also hidden for the same reason 